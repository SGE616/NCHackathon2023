var card = document.querySelectorAll('.card');
card[0].addEventListener( 'click', async() => {
  card[0].classList.toggle('is-flipped'); });
card[1].addEventListener( 'click', async() => {
  card[1].classList.toggle('is-flipped'); });
